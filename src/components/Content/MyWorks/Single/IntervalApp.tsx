import ProjectCardPage from "./ProjectCardPage";
import interval from "../../../../assets/project/interval.png";

const IntervalApp = () => <ProjectCardPage index={23} img={interval} />;

export default IntervalApp;
