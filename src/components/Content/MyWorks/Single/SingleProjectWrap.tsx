import styled from "styled-components";
import { WORK_CARD_WIDTH } from "../WorksCards/WorkCards";

const SingleProjectWrap = styled.div`
  width: ${WORK_CARD_WIDTH}px;
  max-width: 100%;
  margin: 0 auto;
`;

export default SingleProjectWrap;
